/* eslint-disable */
import Stack from '../index.js';

describe('tests for method clone', () => {
  let stack;

  beforeEach(() => {
    stack = new Stack();
    stack.push(1);
    stack.push(2);
    stack.push(3);

  });

  test('method return new stack ', () => {
    
  });
});
